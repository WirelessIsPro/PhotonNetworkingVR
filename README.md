Photon Networking System for VR!

wireless made this ye!
Discord for Support/Help: https://discord.gg/cq7EUWtefC

If Not Using the Unity Package
Place the NetworkingVR Folder in Resources
caa boom it's imported

to use
Create a GameObject in the Scene
Add NetworkManager
Assign all Hand Transforms(Head = Main Camera, Body = Body Collider(Capsule Under Camera) Left Hand and Right Hand are controller objects
NetworkPlayer Path needs to be assigned(starts the path in resources so by default it's NertworkingVR/NetworkPlayer

Instance is Automatically Set

if photon pun and voice has been set up your done
